version: '3.8'

services:
  postgres:
    container_name: english_exercise
    image: postgres:16
    environment:
      POSTGRES_DB: english_exercise_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "6000:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
